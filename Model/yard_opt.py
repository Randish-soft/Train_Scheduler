from ortools.linear_solver import pywraplp

def optimise_yards(timetable, yard_candidates, max_yards):
    # binary open/close vars + assignment vars
    # objective = Σ empty_km * assignment
    return updated_yard_locations_df
