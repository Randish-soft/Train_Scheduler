from ortools.graph import pywrapgraph

def allocate_trains(G, demand_matrix):
    # build a min-cost flow network and solve
    return train_timetable_df   # rows: train_id, dep_city, dep_time, arr_city, …
